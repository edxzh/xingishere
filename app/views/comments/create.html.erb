<% if @comments.present? %>
  <% @comments.each_with_index do |comment, index| %>
    <%= raw show_comment(comment, @comments.count - index - 1) %>
  <% end %>
  <%#= paginate @comments %>
<% else %>
  还没有人留下痕迹，还不快来抢沙发！！！！！！！
  <img src="/images/shafa.jpg" alt="" />
<% end %>
