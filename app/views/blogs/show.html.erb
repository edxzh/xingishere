<% provide(:title, '博客') %>
<ol class="breadcrumb">
  <li><%= link_to '首页', '/' %></li>
  <li><%= link_to @blog.category_name, blogs_path(category: @blog.blog_category_id) %></li>
  <li class="active">正文</li>
</ol>
<div class="blog">
  <div class="panel-heading">
    <span>博客</span>
    <%= link_to "返回博客列表", blogs_path, class: "pull-right" %>
  </div>
  <h2 class="title"><%= @blog.title %></h2>
    <div class="blog_info">
      <%= hidden_field_tag :blog_id, @blog.id, id: "blog_id" %>
      作者：<%= @blog.username %> | 
      标签：<% @blog.tags.each do |tag| %>
              <%= link_to @blog.tag, root_path(tags: @blog.category) %><br />
            <% end %>
      | 发布：<%= format_time(@blog.created_at) %>
      <% if @auth.present? %>
      <% end %>
    </div>
  <div class="content"><%= raw @blog.content %></div>
  <div class="mb40">
    <span onclick="user_like(<%= @blog.id %>)" class="pull-right">
      <a href="javascript:void(0)" id="like"><%= like_icon %><span id="count"><%= @blog.loves_count %></span>人喜欢</a>
    </span>
    <div class="cb"></div>
  </div>
</div>
<form action="" method="post">
  <textarea name="" class="cmt_text_area"></textarea>
  <br />
  <input type="button" class="btn btn-primary cmt_btn" value="发表" name="" />
</form>
<p class="message_title">评论（<%= @comments.length %>）</p>
<div class="cmt_box">
  <% if @comments.present? %>
    <% @comments.each_with_index do |comment, index| %>
      <%= raw show_comment(comment, @comments.count - index -1) %>
    <% end %>
    <%= paginate @comments %>
  <% else %>
    还没有人留下痕迹，还不快来抢沙发！！！！！！！
    <img src="/images/shafa.jpg" alt="" />
  <% end %>
</div>

